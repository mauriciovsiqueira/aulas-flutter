// Constantes na Prática: Crie um programa que calcule a área de um círculo (A = pi  r * r). Defina o pi como const e o raio como final.

main() {
  const pi = 3.14;
  final raio = 4;

  var resultado = pi * raio * raio;

  print(resultado);
}