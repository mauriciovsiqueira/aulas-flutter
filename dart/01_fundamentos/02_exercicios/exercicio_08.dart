// Inspeção de Tipos: Crie uma variável usando var e atribua um valor lógico a ela. Use a propriedade .runtimeType impressa no console para verificar o tipo detectado pelo Dart.

void main() {
  var tipo = true;

  print(tipo.runtimeType);
}
